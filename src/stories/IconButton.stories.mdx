import { ArgsTable, Story, Canvas, Meta } from '@storybook/addon-docs';
import Button from '@mui/material/Button';
import IconButton from '@mui/material/IconButton';
import Stack from '@mui/material/Stack';
import DeleteIcon from '@mui/icons-material/Delete';
import SendIcon from '@mui/icons-material/Send';
import SearchIcon from '@mui/icons-material/Search';

<Meta title='Base/Buttons/Icon Buttons' component={IconButton} />

# Icon Button

Icon buttons are commonly found in app bars and toolbars.

Icons are also appropriate for toggle buttons that allow a single choice to be selected or deselected, such as adding or removing a star to an item.

[IconButton Props](https://mui.com/material-ui/api/icon-button/#props)

<!------------------------------------| IMPORT |---------------------------------------->

## Import

```jsx
import { IconButton } from 'design-system';
```

<!------------------------------------| STORIES |--------------------------------------->

## Stories

### Default

<Canvas>
    <Story name='Default'>
        <IconButton aria-label='search'>
            <SearchIcon />
        </IconButton>
    </Story>
</Canvas>

### Icon Variants

<Canvas>
    <Story name='Icon Variants'>
        <Stack direction='row' spacing={1}>
            <IconButton aria-label='delete' variant='contained'>
                <SearchIcon />
            </IconButton>
            <IconButton aria-label='delete' variant='outlined'>
                <SearchIcon />
            </IconButton>
            <IconButton aria-label='delete' variant='icon'>
                <SearchIcon />
            </IconButton>
        </Stack>
    </Story>
</Canvas>

### Icon Variants Disabled

<Canvas>
    <Story name='Icon Variants Disabled'>
        <Stack direction='row' spacing={1}>
            <IconButton disabled aria-label='search' variant='contained'>
                <SearchIcon />
            </IconButton>
            <IconButton disabled aria-label='search' variant='outlined'>
                <SearchIcon />
            </IconButton>
            <IconButton disabled aria-label='search' variant='icon'>
                <SearchIcon />
            </IconButton>
        </Stack>
    </Story>
</Canvas>

### Text and Icon Buttons

<Canvas>
    <Story name='Text and Icon Buttons'>
        <Stack direction='row' spacing={2}>
            <Button variant='outlined' startIcon={<DeleteIcon />}>
                Delete
            </Button>
            <Button variant='contained' endIcon={<SendIcon />}>
                Send
            </Button>
        </Stack>
    </Story>
</Canvas>
